export async function buscarRankingVias(
    tipo?: string | null,
    anos?: number[] | null,
    estado?: number | null,
    pais?: number | null,
    ncm?: number | null,
    urf?: number | null,
    cresc?: boolean | null
) {
    
}